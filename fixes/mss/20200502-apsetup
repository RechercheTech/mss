tarball=apsetup.tar
serverip=repo.myscoolserver.com
wget http://$serverip/fixes/$tarball
tar xf $tarball -C / && rm $tarball
chmod +x /usr/bin/apsetup
echo "`date`: $tarball applied" >> /var/log/fixlog
